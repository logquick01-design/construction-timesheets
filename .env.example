# Local development
DATABASE_URL="file:./dev.db"

# Production (Railway): add a volume at /data — DATABASE_URL is set automatically
# at startup from the mounted volume. Only override if you use a custom mount path:
# DATABASE_URL="file:/data/prod.db"

AUTH_SECRET="change-me-to-a-long-random-string-in-production"
